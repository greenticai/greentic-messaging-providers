{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Telegram Provider-Core Config",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "enabled",
    "public_base_url",
    "api_base_url"
  ],
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Enable the Telegram provider."
    },
    "public_base_url": {
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "description": "Public base URL for inbound webhook callbacks."
    },
    "default_chat_id": {
      "type": "string",
      "description": "Default chat id used when an invocation does not supply one."
    },
    "api_base_url": {
      "type": "string",
      "format": "uri",
      "description": "Override for the Telegram API base URL.",
      "default": "https://api.telegram.org"
    },
    "bot_token": {
      "type": "string",
      "description": "Optional Telegram bot token. Falls back to secret store if omitted.",
      "x-redact": true
    }
  }
}
