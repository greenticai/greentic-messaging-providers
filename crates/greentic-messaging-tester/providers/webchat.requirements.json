{
  "provider": "webchat",
  "config": {
    "required": [
      { "key": "public_base_url", "type": "string", "example": "https://example.com/webhooks/webchat" }
    ]
  },
  "secrets": {
    "required": [
      { "key": "jwt_signing_key", "example": "jwt-secret" }
    ]
  },
  "values": {
    "config": {
      "route": "webchat-route",
      "tenant_channel_id": "tenant-channel",
      "public_base_url": "https://example.com/webhooks/webchat"
    },
    "secrets": {
      "jwt_signing_key": "dummy-jwt-secret"
    },
    "http": "mock",
    "state": {}
  }
}
