{
  "provider": "webex",
  "config": {
    "required": [
      { "key": "public_base_url", "type": "string", "example": "https://example.com" }
    ]
  },
  "secrets": {
    "required": [
      { "key": "WEBEX_BOT_TOKEN", "example": "xxxxx" }
    ]
  },
  "values": {
    "config": {
      "public_base_url": "https://example.com",
      "api_base_url": "https://webexapis.com/v1"
    },
    "secrets": {
      "WEBEX_BOT_TOKEN": "dummy-token"
    },
    "to": {
      "room_id": "room123",
      "person_id": "user123"
    },
    "http": "mock",
    "state": {}
  }
}
