{
  "provider": "slack",
  "config": {
    "required": [
      { "key": "api_base", "type": "string", "example": "https://slack.com/api" }
    ]
  },
  "secrets": {
    "required": [
      { "key": "SLACK_BOT_TOKEN", "example": "xoxb-..." }
    ]
  },
  "values": {
    "config": {
      "api_base": "https://slack.com/api"
    },
    "secrets": {
      "SLACK_BOT_TOKEN": "xoxb-dummy"
    },
    "http": "mock",
    "state": {}
  }
}
