{
  "provider": "teams",
  "config": {
    "required": [
      { "key": "tenant_id", "type": "string", "example": "tenant" },
      { "key": "client_id", "type": "string", "example": "client" }
    ]
  },
  "secrets": {
    "required": [
      { "key": "MS_GRAPH_CLIENT_SECRET", "example": "dummy-secret" },
      { "key": "MS_GRAPH_REFRESH_TOKEN", "example": "dummy-refresh" }
    ]
  },
  "to": {
    "shape": {
      "team_id": "team-sync",
      "channel_id": "general"
    }
  },
  "values": {
    "config": {
      "tenant_id": "example-tenant",
      "client_id": "example-client"
    },
    "secrets": {
      "MS_GRAPH_CLIENT_SECRET": "dummy-secret",
      "MS_GRAPH_REFRESH_TOKEN": "dummy-refresh"
    },
    "to": {
      "team_id": "team-sync",
      "channel_id": "general"
    },
    "http": "mock",
    "state": {}
  }
}
