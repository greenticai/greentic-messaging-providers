name: Build, Test, and Publish Packs

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  OCI_REGISTRY: ghcr.io
  OCI_NAMESPACE: ${{ github.repository_owner }}/greentic-messaging-providers

jobs:
  install-tools:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip2
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: build-test-publish

      - name: Check component operation schemas
        run: python3 tools/check_op_schemas.py

      - name: Cache cargo bin tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin
          key: cargo-bin-tools-${{ runner.os }}-v1

      - name: Install cargo-binstall
        run: |
          if ! command -v cargo-binstall >/dev/null 2>&1; then
            cargo install cargo-binstall --locked
          fi

      - name: Install cargo-component
        run: |
          if ! command -v cargo-component >/dev/null 2>&1; then
            cargo binstall cargo-component --no-confirm --locked || cargo install cargo-component --locked
          fi

      - name: Install greentic-pack
        env:
          CARGO_NET_GIT_FETCH_WITH_CLI: "true"
        run: |
          if ! command -v greentic-pack >/dev/null 2>&1; then
            cargo binstall greentic-pack --no-confirm --locked || cargo install greentic-pack --locked
          fi
          echo "${HOME}/.cargo/bin" >> "${GITHUB_PATH}"
          greentic-pack --version

      - name: Install greentic-flow
        run: |
          if ! command -v greentic-flow >/dev/null 2>&1; then
            cargo binstall greentic-flow --no-confirm --locked || cargo install greentic-flow --locked
          fi

      - name: Install greentic-runner
        run: |
          if ! command -v greentic-runner >/dev/null 2>&1; then
            cargo binstall greentic-runner --no-confirm --locked || cargo install greentic-runner --locked
          fi

      - name: Install greentic-component
        run: |
          if ! command -v greentic-component >/dev/null 2>&1; then
            cargo binstall greentic-component --no-confirm --locked || cargo install greentic-component --locked
          fi

      - name: Install oras
        uses: oras-project/setup-oras@v1
        with:
          version: 1.2.0

  build-components:
    runs-on: ubuntu-latest
    needs: install-tools
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint for legacy HTTP imports
        run: |
          if rg -n 'greentic:http/http-client@' components/ -S; then
            echo "legacy greentic:http/http-client@ import still present in components/"
            exit 1
          fi

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip2
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: build-test-publish

      - name: Cache cargo bin tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin
          key: cargo-bin-tools-${{ runner.os }}-v1

      - name: Build components
        run: ./tools/build_components.sh

      - name: Upload component artifacts
        uses: actions/upload-artifact@v4
        with:
          name: components-wasm
          path: target/components/*.wasm
          if-no-files-found: error

  cargo-fmt:
    runs-on: ubuntu-latest
    needs: install-tools
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: build-test-publish

      - name: Ensure rustfmt is installed
        run: rustup component add rustfmt

      - name: Cargo fmt
        run: cargo +stable fmt --check

  cargo-test:
    runs-on: ubuntu-latest
    needs: install-tools
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: build-test-publish

      - name: Cargo test
        run: cargo test --workspace

  pack-and-validate:
    runs-on: ubuntu-latest
    needs: [build-components, cargo-fmt, cargo-test]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip2
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: build-test-publish

      - name: Cache cargo bin tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin
          key: cargo-bin-tools-${{ runner.os }}-v1

      - name: Install cargo-binstall
        run: |
          if ! command -v cargo-binstall >/dev/null 2>&1; then
            cargo install cargo-binstall --locked
          fi

      - name: Install cargo-component
        run: |
          if ! command -v cargo-component >/dev/null 2>&1; then
            cargo binstall cargo-component --no-confirm --locked || cargo install cargo-component --locked
          fi

      - name: Install greentic-pack
        env:
          CARGO_NET_GIT_FETCH_WITH_CLI: "true"
        run: |
          if ! command -v greentic-pack >/dev/null 2>&1; then
            cargo binstall greentic-pack --no-confirm --locked || cargo install greentic-pack --locked
          fi
          echo "${HOME}/.cargo/bin" >> "${GITHUB_PATH}"
          greentic-pack --version

      - name: Install greentic-flow
        run: |
          if ! command -v greentic-flow >/dev/null 2>&1; then
            cargo binstall greentic-flow --no-confirm --locked || cargo install greentic-flow --locked
          fi

      - name: Install greentic-runner
        run: |
          if ! command -v greentic-runner >/dev/null 2>&1; then
            cargo binstall greentic-runner --no-confirm --locked || cargo install greentic-runner --locked
          fi

      - name: Install greentic-component
        run: |
          if ! command -v greentic-component >/dev/null 2>&1; then
            cargo binstall greentic-component --no-confirm --locked || cargo install greentic-component --locked
          fi

      - name: Install oras
        uses: oras-project/setup-oras@v1
        with:
          version: 1.2.0

      - name: Packgen gtests
        env:
          RUN_PACKGEN_TESTS: "1"
        run: cargo test -p greentic-messaging-packgen --tests

      - name: Download component artifacts
        uses: actions/download-artifact@v4
        with:
          name: components-wasm
          path: target/components

      - name: Generate flows (packgen)
        run: ./ci/gen_flows.sh

      - name: Check generated flows
        run: ./ci/check_generated.sh

      - name: Sync packs
        run: ./tools/sync_packs.sh

      - name: Validate flows
        run: |
          for f in packs/*/flows/*.ygtc; do
            greentic-flow doctor "$f"
          done

      - name: Validate components
        run: |
          for c in packs/*/components/*.manifest.json; do
            greentic-component doctor "$c"
          done

      - name: Test component-questions
        run: |
          set -euo pipefail
          tmpdir="$(mktemp -d)"
          trap 'rm -rf "${tmpdir}"' EXIT
          cp components/questions/questions.wasm "${tmpdir}/questions.wasm"
          cp components/questions/component.manifest.json "${tmpdir}/component.manifest.json"
          cp packs/messaging-dummy/setup.yaml "${tmpdir}/setup.yaml"
          (
            cd "${tmpdir}"
            greentic-component test \
              --wasm questions.wasm \
              --manifest component.manifest.json \
              --op emit \
              --input-json '{"id":"dummy-setup","spec_ref":"assets/setup.yaml","context":{"tenant_id":"t1","env":"dev"}}'
          )
          python3 - <<'PY' "${tmpdir}"
          import json
          import sys
          from pathlib import Path

          tmpdir = Path(sys.argv[1])
          spec = {
              "provider_id": "dummy",
              "version": 1,
              "title": "Dummy provider setup",
              "questions": [],
              "id": "dummy-setup",
          }
          payload = {
              "id": "dummy-setup",
              "spec_json": json.dumps(spec),
              "answers_json": json.dumps({}),
          }
          (tmpdir / "validate_input.json").write_text(json.dumps(payload))
          PY
          (
            cd "${tmpdir}"
            greentic-component test \
              --wasm questions.wasm \
              --manifest component.manifest.json \
              --op validate \
              --input validate_input.json
          )

      - name: Pack dry-run (no push)
        env:
          DRY_RUN: "1"
          PACK_VERSION: "0.0.0-dev"
        run: ./tools/publish_packs_oci.sh

      - name: Runner-desktop setup smoke
        run: cargo test -p provider-common runner_desktop_setup_default_smoke

      - name: Validate pack archives
        run: |
          echo "provider pack must pass greentic-pack doctor (messaging validator)"
          for p in dist/packs/messaging-*.gtpack; do
            greentic-pack doctor --validate --validator-pack "oci://ghcr.io/greentic-ai/validators/messaging:latest" --pack "$p"
          done

      - name: Upload packs + fixtures report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: packs-and-fixtures
          path: |
            dist/packs/*.gtpack
            dist/fixtures_report.json
          if-no-files-found: error

      - name: Upload packs lockfile (dry-run)
        uses: actions/upload-artifact@v4
        with:
          name: packs-lockfile-dryrun
          path: packs.lock.json
          if-no-files-found: error

  publish:
    runs-on: ubuntu-latest
    needs: pack-and-validate
    if: github.event_name == 'push' && (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/'))
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip2
          components: rustfmt, clippy

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: build-test-publish

      - name: Cache cargo bin tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin
          key: cargo-bin-tools-${{ runner.os }}-v1

      - name: Install cargo-binstall
        run: |
          if ! command -v cargo-binstall >/dev/null 2>&1; then
            cargo install cargo-binstall --locked
          fi

      - name: Install greentic-pack
        env:
          CARGO_NET_GIT_FETCH_WITH_CLI: "true"
        run: |
          if ! command -v greentic-pack >/dev/null 2>&1; then
            cargo binstall greentic-pack --no-confirm --locked || cargo install greentic-pack --locked
          fi
          echo "${HOME}/.cargo/bin" >> "${GITHUB_PATH}"
          greentic-pack --version

      - name: Install oras
        uses: oras-project/setup-oras@v1
        with:
          version: 1.2.0

      - name: Download component artifacts
        uses: actions/download-artifact@v4
        with:
          name: components-wasm
          path: target/components

      - name: Set publish version
        run: |
          python3 - <<'PY'
          import os
          import tomllib
          from pathlib import Path

          data = tomllib.loads(Path("Cargo.toml").read_text())
          version = data["workspace"]["package"]["version"]
          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as handle:
            handle.write(f"PUBLISH_VERSION={version}\n")
          print(f"Using Cargo workspace version: {version}")
          PY

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Publish components
        env:
          VERSION: ${{ env.PUBLISH_VERSION }}
        run: bash ./tools/publish_oci.sh

      - name: Upload components lockfile
        uses: actions/upload-artifact@v4
        with:
          name: components-lockfile
          path: components.lock.json
          if-no-files-found: error

      - name: Publish packs
        env:
          PACK_VERSION: ${{ env.PUBLISH_VERSION }}
        run: ./tools/publish_packs_oci.sh

      - name: Upload built packs
        uses: actions/upload-artifact@v4
        with:
          name: gtpack-artifacts
          path: dist/packs/*.gtpack
          if-no-files-found: error
