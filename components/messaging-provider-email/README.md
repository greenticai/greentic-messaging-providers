# Messaging Provider Email Component

Provider-core SMTP email sender (simulated).

## Component ID
- `messaging-provider-email`

## Provider types
- `messaging.email.smtp`

## Secrets
- `EMAIL_PASSWORD` (tenant): SMTP password secret key
