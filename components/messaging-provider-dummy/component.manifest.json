{
  "name": "messaging-provider-dummy",
  "description": "Deterministic provider-core messaging provider for CI fixtures.",
  "config_schema": {
    "provider_config": {
      "format": "json",
      "path": "schemas/messaging/dummy/public.config.schema.json"
    }
  },
  "secret_requirements": [
    {
      "name": "DUMMY_API_TOKEN",
      "scope": "tenant",
      "description": "Dummy provider token (redacted in describe/config metadata)."
    }
  ]
}
