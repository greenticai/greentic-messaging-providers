# Secrets Probe Component

Minimal component that probes greentic:secrets-store for TEST_API_KEY.

## Component ID
- `secrets-probe`

## Secrets
- `TEST_API_KEY` (tenant): API key required to validate secrets-store integration.
