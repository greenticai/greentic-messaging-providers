# Messaging Ingress WhatsApp Component

Ingress-only WhatsApp component for webhook validation and normalization.

## Component ID
- `messaging-ingress-whatsapp`

## Provider types
- `messaging.whatsapp.cloud`

## Secrets
- `WHATSAPP_VERIFY_TOKEN` (tenant): Verify token used for WhatsApp webhook validation (if configured).
