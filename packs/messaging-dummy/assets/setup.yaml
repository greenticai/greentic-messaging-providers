provider_id: dummy
version: 1
title: Dummy provider setup
questions:
  - name: api_token
    title: API token
    kind: string
    required: true
    secret: true
  - name: endpoint_url
    title: Endpoint URL
    kind: string
    required: true
    validate:
      regex: "^https?://"
