---
# Requirements check flow for messaging-whatsapp provider.
# Validates that required configuration is in place.
id: requirements
title: WhatsApp Requirements Check
description: Validates WhatsApp messaging provider configuration
version: "1"

nodes:
  - id: check_config
    type: component
    component: messaging-provider-whatsapp
    op: messaging.configure
    input: {}

edges: []
