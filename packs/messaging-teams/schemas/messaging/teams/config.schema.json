{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Teams Bot Service Provider Config",
  "type": "object",
  "properties": {
    "ms_bot_app_id": {
      "type": "string",
      "description": "Microsoft Bot App ID from Azure Bot registration."
    },
    "public_base_url": {
      "type": "string",
      "format": "uri",
      "description": "Public base URL for Bot Framework messaging endpoint."
    },
    "default_service_url": {
      "type": "string",
      "format": "uri",
      "description": "Default Bot Connector service URL for proactive messages."
    },
    "team_id": {
      "type": "string",
      "description": "Default team ID if not provided in invocation."
    },
    "channel_id": {
      "type": "string",
      "description": "Default channel ID if not provided in invocation."
    }
  },
  "required": ["ms_bot_app_id", "public_base_url"],
  "additionalProperties": false
}
