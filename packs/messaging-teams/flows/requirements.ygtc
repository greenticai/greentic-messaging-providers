---
# Requirements check flow for messaging-teams provider.
# Validates that required configuration is in place.
id: requirements
title: Teams Requirements Check
description: Validates Teams messaging provider configuration
version: "1"

nodes:
  - id: check_config
    type: component
    component: messaging-provider-teams
    op: messaging.configure
    input: {}

edges: []
