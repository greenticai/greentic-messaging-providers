[
  {
    "key": "SLACK_BOT_TOKEN",
    "required": true,
    "description": "Slack bot token used for chat.postMessage calls.",
    "scope": {
      "env": "<env>",
      "tenant": "<tenant>"
    }
  },
  {
    "key": "SLACK_SIGNING_SECRET",
    "required": true,
    "description": "Slack signing secret used to verify webhook signatures (optional).",
    "scope": {
      "env": "<env>",
      "tenant": "<tenant>"
    }
  }
]