id: setup_custom
type: job
in: setup_custom__collect
parameters: {}
tags: []
schema_version: 2
entrypoints: {}
nodes:
  setup_custom__collect:
    routing:
    - to: setup_custom__validate
    text:
      config:
        public_base_url: "{{ public_base_url }}"
        default_room_id: "{{ default_room_id }}"
      text: 'Collect inputs for setup_custom.

        Required config keys: ["public_base_url"]

        Optional config keys: ["default_room_id"]

        Required secrets: ["WEBEX_BOT_TOKEN"]

        Optional secrets: []

        Webex bots register webhooks for inbound.

        '
  setup_custom__validate:
    routing:
    - to: setup_custom__apply
    text:
      config:
        public_base_url: "{{ public_base_url }}"
        default_room_id: "{{ default_room_id }}"
      text: 'Validate inputs. In dry-run mode, do not call external APIs.

        Live mode may validate credentials with provider APIs when host-gated.

        '
  setup_custom__apply:
    routing:
    - to: setup_custom__summary
    text:
      config:
        public_base_url: "{{ public_base_url }}"
        default_room_id: "{{ default_room_id }}"
      text: "Apply changes.\nDry-run must return a deterministic ProvisionPlan and\
        \ perform no network calls.\n\nProvisionPlan (JSON):\n{\n  \"config_patch\"\
        : {\"public_base_url\": \"<value>\", \"default_room_id\"\
        : \"<value>\"},\n  \"secrets_patch\": { \"set\": {\"WEBEX_BOT_TOKEN\": \"\
        <redacted>\"}, \"delete\": [] },\n  \"webhook_ops\": [{\"op\": \"register\"\
        , \"url\": \"<public_base_url>/webhooks/webex\"}],\n  \"subscription_ops\"\
        : [],\n  \"oauth_ops\": []\n}\n"
  setup_custom__summary:
    routing: out
    text:
      config:
        public_base_url: "{{ public_base_url }}"
        default_room_id: "{{ default_room_id }}"
      text: messaging.webex.bot setup_custom complete.
