---
# Requirements check flow for messaging-webex provider.
# Validates that required configuration is in place.
id: requirements
title: Webex Requirements Check
description: Validates Webex messaging provider configuration
version: "1"

nodes:
  - id: check_config
    type: component
    component: messaging-provider-webex
    op: messaging.configure
    input: {}

edges: []
