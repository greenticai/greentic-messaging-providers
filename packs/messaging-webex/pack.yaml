pack_id: messaging-webex
version: 0.4.8
kind: application
publisher: Greentic

components:
  - id: "messaging-provider-webex"
    kind: messaging
    version: 0.4.8
    world: "greentic:provider-schema-core/schema-core@1.0.0"
    supports: ["messaging"]
    profiles:
      default: "default"
      supported: ["default"]
    capabilities:
      wasi: {}
      host: {}
    wasm: "components/messaging-provider-webex.wasm"

extensions:
  greentic.ext.provider:
    inline:
      providers:
        - provider_type: "messaging.webex.bot"
          capabilities: []
          ops: ["send", "reply"]
          config_schema_ref: "schemas/messaging/webex/config.schema.json"
          state_schema_ref: null
          runtime:
            component_ref: "messaging-provider-webex"
            export: "schema-core-api"
            world: "greentic:provider-schema-core/schema-core@1.0.0"
          docs_ref: null

schemas:
  config: "schemas/messaging/webex/config.schema.json"

secret_requirements:
  - name: "WEBEX_BOT_TOKEN"
    scope: "tenant"
    description: "Webex bot access token"

dependencies: []

assets: []
